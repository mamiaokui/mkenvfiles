#!/usr/bin/python
import sys
import os
shell = "/Applications/Emacs.app/Contents/MacOS/Emacs "
argv = ""

if len(sys.argv) == 1:
    argv = "."
else:
    for i in range(1, len(sys.argv)):
        argv += sys.argv[i] + " "

os.system(shell + argv)
    
            
        
